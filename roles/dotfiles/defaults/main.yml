---
# Dotfiles management configuration
dotfiles_repo: "https://github.com/snakagawax/dotfiles.git"
dotfiles_dir: "{{ ghq_root }}/snakagawax/dotfiles"
ghq_root: "{{ ansible_env.HOME }}/ghq/github.com"

# Symbolic link mappings
dotfiles_links:
  - src: ".gitconfig"
    dest: "{{ ansible_env.HOME }}/.gitconfig"
    
  - src: ".gitignore_global"
    dest: "{{ ansible_env.HOME }}/.gitignore_global"
    
  - src: ".inputrc"
    dest: "{{ ansible_env.HOME }}/.inputrc"
    
  - src: ".config/fish"
    dest: "{{ ansible_env.HOME }}/.config/fish"
    
  - src: ".config/karabiner"
    dest: "{{ ansible_env.HOME }}/.config/karabiner"

# Backup settings
backup_enabled: true
backup_suffix: ".ansible.backup"